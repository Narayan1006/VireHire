# =============================================================================
# VeriHire AI — Frontend Environment
#
# Copy this file to .env and set the URL of your Spring Boot backend.
# NEVER commit a .env with real production URLs to version control.
# =============================================================================

# Spring Boot backend URL (NO trailing slash)
# Local dev:
VITE_API_URL=http://localhost:8080
# Production (set to your deployed Spring Boot URL):
# VITE_API_URL=https://api.verehire.ai
